Talvez seja melhor aplicar o CSS de transformação diretamente no elemento via ref.current.style.transform = ... durante o arraste, e só no “final” atualizar o contexto.

colocar a transformação em aoMover dentro de um requestAnimationFrame