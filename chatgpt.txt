Em vez de usar, transformação diretamente no elemento via ref.current.style.transform = ... durante o arraste, e só no “final” atualizar o contexto e colocar a transformação em aoMover dentro de um requestAnimationFrame (se possivel). 